import React from 'react';

const NotFoundPage: React.FC = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Não autorizado
    </div>
  );
};

export default NotFoundPage;
